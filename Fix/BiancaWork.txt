Bianca:
pagina general private:

ELIMINARE titolo benvenuto Ciccio78

FORM MODIFICA CONTATTI:	
	STILE:	sistemare larghezza dei campi di input, centrateli

	ACCESSIBILITA:	inserire tutti i campi della form dentro ad un div e controllate gli attributi della form, ogni scritta
					deve essere una LABEL,per maggiori informazioni cercate in rete o contatta il gruppo.

FORM MODIFICA PASSWORD:
	STILE:	sistemare larghezza dei campi di input, centrateli
	
	ACCESSIBILITA:	inserire tutti i campi della form dentro ad un div e controllate gli attributi della form, ogni scritta
					deve essere una LABEL,per maggiori informazioni cercate in rete o contatta il gruppo.


FORM MODIFICA LOGO:
	STILE:	sistemare larghezza dei campi di input, pulsanti scritte e campo di input devono stare sulla stessa riga
	
	STRUTTURA:	la struttura di questo deve essere sistemata, eliminate il campo reset, ogni scritta deve essere una LABEL

	ACCESSIBILITA:	inserire tutti i campi della form dentro ad un div e controllate gli attributi della form, ogni scritta
					deve essere una LABEL,per maggiori informazioni cercate in rete o contatta il gruppo.

FORM NOME NEGOZIO:
	STILE:	sistemare larghezza dei campi di input, pulsanti scritte e campo di input devono stare sulla stessa riga
			allargare la form facendo si che occupi il 50% della pagina, quindi ci stiano 2 form in orizzontale
				
	ACCESSIBILITA:	inserire tutti i campi della form dentro ad un div e controllate gli attributi della form, ogni scritta
					deve essere una LABEL,per maggiori informazioni cercate in rete o contatta il gruppo.

FORM MODIFICA ORARIO:
	STILE:	sistemare larghezza dei campi di input

	ACCESSIBILITA:	inserire tutti i campi della form dentro ad un div e controllate gli attributi della form, ogni scritta
					deve essere una LABEL,per maggiori informazioni cercate in rete o contatta il gruppo.

FORM MODIFICA DESCRIZIONE:
	ACCESSIBILITA:	inserire tutti i campi della form dentro ad un div e controllate gli attributi della form, ogni scritta
					deve essere una LABEL,per maggiori informazioni cercate in rete o contatta il gruppo.

pagina promozioni private:
	
FORM NUOVA PROMOZIONE:	
	STRUTTURA:	inserire un campo di input, questo sarà il campo alt della promozione,
					inserire una opportuna label al campo inserito.
				
	ACCESSIBILITA:	inserire tutti i campi della form dentro ad un div e controllate gli attributi della form, ogni scritta
					deve essere una LABEL,per maggiori informazioni cercate in rete o contatta il gruppo.

pagina prodotti private:
	
FORM NUOVO PRODOTTO:	
	STRUTTURA:	inserire un campo di input, questo sarà il campo titolo del prodotto,
					inserire una opportuna label al campo inserito.
				inserire un campo di input, questo sarà il campo alt del prodotto,
					inserire una opportuna label al campo inserito.
				
	ACCESSIBILITA:	inserire tutti i campi della form dentro ad un div e controllate gli attributi della form, ogni scritta
					deve essere una LABEL,per maggiori informazioni cercate in rete o contatta il gruppo.


Sistemate in generale le pagine in modo che siano definitive, se avete dubbi o altro scrivete nel gruppo ;)